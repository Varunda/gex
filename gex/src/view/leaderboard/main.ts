import Vue from "vue";

import Leaderboard from "./Leaderboard.vue";

const vm = new Vue({
	el: "#app",

	created: function(): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		Leaderboard
	}
});
(window as any).vm = vm;
