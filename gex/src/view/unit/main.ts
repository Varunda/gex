import Vue from "vue";

import BarUnitView from "./BarUnit.vue";

const vm = new Vue({
	el: "#app",

	created: function(): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		BarUnitView
	}
});
(window as any).vm = vm;
