import Vue from "vue";

import FixMatches from "./FixMatches.vue";

const vm = new Vue({
	el: "#app",

	created: function (): void {

	},

	data: {

	},

	methods: {

	},

	components: {
		FixMatches
	}
});
(window as any).vm = vm;
