import Vue from "vue";

import MatchPoolView from "./MatchPoolView.vue";

const vm = new Vue({
	el: "#app",

	created: function(): void {

	},

	data: {

	},

	methods: {

	},

	components: {
        MatchPoolView
	}
});
(window as any).vm = vm;
